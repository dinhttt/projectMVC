@model IEnumerable<ContosoUniversity.Models.ThongtinSuCo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ThongTinSuCoCode)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CreateDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ThongTinSuCoCode)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CreateDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ThongtinSuCoID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ThongtinSuCoID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ThongtinSuCoID })
        </td>
    </tr>
}

</table>
